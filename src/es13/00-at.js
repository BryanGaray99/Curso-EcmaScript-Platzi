// Clase 33 - At con arrays

const arreglo = [1, 2, 3, 4, 5];

// Antes de es13
console.log(arreglo[arreglo.length -1]);
// Con at de es13 se usa el método at y adentro la posición 
// si usamos índices negativos "-1" representa la última posición
console.log(arreglo.at(-1));